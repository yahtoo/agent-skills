description = "用测试和运行时验证证明行为正确"
prompt = """
调用 test-driven-development skill。

围绕当前变更或 bug 执行证明流程：

1. 先写能失败的测试或复现用例
2. 运行测试并确认失败原因与预期一致
3. 实现最小修复或最小功能
4. 重新运行目标测试并确认通过
5. 运行相关测试套件，检查回归
6. 必要时运行 build 或手动验证

对于浏览器相关问题，同时调用 browser-testing-with-devtools，使用 Chrome DevTools MCP 验证运行时行为。
"""
